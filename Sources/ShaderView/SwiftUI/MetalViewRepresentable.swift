//
//  MetalViewRepresentable.swift
//  
//
//  Created by Pirita Minkkinen on 12/5/23.
//

import Foundation
import SwiftUI
import MetalKit

/*
// conditionally returns the correct view for the platform.
struct MetalViewRepresentable: View {
    var drawableSize: CGSize
    var shaderViewModel: ShaderViewModel

    var body: some View {
        #if os(macOS)
        // macOS specific view
        MetalNSViewRepresentable(drawableSize: drawableSize, shaderViewModel: shaderViewModel)
        #else
        // iOS specific view
        MetalUIViewRepresentable(drawableSize: drawableSize, shaderViewModel: shaderViewModel)
        #endif
    }
}
*/
